# Production Environment
NODE_ENV=production
PORT=5000

# CORS Configuration - CRITICAL: Update with your actual domains
FRONTEND_URL=https://your-actual-domain.com
FRONTEND_URL_WWW=https://www.your-actual-domain.com

# JWT Configuration - MUST use very secure secrets
JWT_SECRET=your-extremely-secure-production-jwt-secret-use-random-64-char-string
JWT_REFRESH_SECRET=your-extremely-secure-refresh-secret-different-64-char-string
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# Optional: Database for production
# DATABASE_URL=mongodb://your-production-db-connection-string/taskflow